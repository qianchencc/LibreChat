# Unified sidebar

- Put workspace-exit navigation in the sidebar rail and mobile drawer header, not the chat header;
  the chat header is reserved for actions on the current conversation.
- The landing-page link is a native `href="/"` so modified clicks retain normal browser behavior.
- Verify persistent rail actions in both desktop and mobile drawer layouts.
