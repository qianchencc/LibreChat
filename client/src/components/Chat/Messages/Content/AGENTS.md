# Message Markdown

- `/mnt/data` is a code-sandbox runtime namespace, not a web route. Render relative and same-origin links into that namespace as non-clickable source locations; generated files reach users through message attachments and Artifact previews.
- Do not block unrelated external URLs merely because their path contains `/mnt/data`.
