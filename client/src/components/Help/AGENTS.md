# Help

- Keep guide structure in `content.ts` and user-facing copy in the English locale file.
- Help media is optional. Images need a stable URL; videos also require a captions URL.
- `/help` is a standalone route: the desktop panel collapses and the route icon stays active.
- Keep `OpenSidebar` in the mobile header so users can leave the page through the main drawer.
